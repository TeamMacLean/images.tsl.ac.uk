node_modules/
config.js
.cache/
# Built bundles are NOT in the repo: run `yarn build` after changing anything
# in public/js/src, and as part of every deploy. (Was the NPM_RUN_BUILD_TOO_GEORGEY
# note below - it sat here as a bare line, which only ignored a file of that name.)
public/js/dist
images/
.DS_Store

# Parcel v2 cache
.parcel-cache/

# Test results and reports
test-results/
playwright-report/
playwright/.cache/

# Uploaded files (runtime data)
files/*

# Babel config (if generated)
.babelrc
babel.config.js

# yarn.lock is committed on purpose - it is what pins the dependency versions.
public/style/style.css

# Yarn
yarn-error.log
.yarn/
.pnp.*
