<%-include('head.ejs') %>

<section class="section">
    <div class="container">
        <h1 class="title"><%= (error && error.message) || error || 'Something went wrong' %></h1>
        <% if (locals.showStack && error && error.stack) { %>
            <pre><%= error.stack %></pre>
        <% } %>
    </div>
</section>

<%-include('foot.ejs') %>
